<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="MigRptFileUtilMapper">

	<delete id="rptFileDeleteAll">
		DELETE FROM top_rpt_file
	</delete>

	<select id="forRptCtrlList" resultType = "kr.co.toplac.sysadm.migration.MigrationRptFileVO">
		SELECT rid, up_dir, up_file, up_date
		, replace(up_file,'|','-breakpoint-') splitTargetData
		FROM ls_report_data
		WHERE length(up_file) > 0
		ORDER BY rid
	</select>

	<insert id="rptFileWrite" parameterType="kr.co.toplac.sysadm.migration.MigrationRptCtrlVO">
		INSERT INTO top_rpt_file (suim_rpt_no, file_path, file_name, reg_date)
		VALUES(#{suim_rpt_no}, #{up_dir}, #{up_file}, #{up_date})
	</insert>

</mapper>
